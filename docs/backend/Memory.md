# Memory — Backend

## Completed

- 2026-09-21 — Backend PRD written (NestJS + Supabase/PostgreSQL) in `docs/backend/PRD.md`
- 2026-09-21 — Docs scaffolded per vibe-coding structure (Architecture, Rules, Phases, Design, Memory)
- 2026-09-23 — Stack dependencies updated in `backend/package.json`: removed Prisma/Razorpay/Nodemailer; added Supabase, Stripe, SSLCommerz, Resend. Docs refreshed to match.
- 2026-09-23 — Added `docs/backend/Database.md` — the feature-wise ERD derived from PRD §3/§4. Table-by-table: a feature-level ERD per PRD feature module, then one section per table (heading + metadata tag + full column spec + **Keys** + **Indexes** + **Constraints** + its own ERD) for all 33 tables. Keys carry PK/UK and every FK with its `on delete` action; §13 is a consolidated Foreign Key Map (86 FKs) and §14 the Index Plan (every index, incl. the mandatory `school_id` index per tenant table). Also enums, transaction boundaries, a table inventory, PRD→table coverage, and 13 open questions (§18). 43 mermaid diagrams total. §19 adds a **full-schema ERD** — one themed diagram (§19) containing all 33 tables, all 86 FK edges and the 2 logical links, themed to Design.md tokens via a mermaid `%%{init}%%` directive. Linked from `AGENTS.md` and `backend/README.md`. No migrations exist yet — every table is a design target.

- 2026-09-23 — **PRD §4 rewritten as endpoint checklists, Phases expanded to match.** Every feature module now lists its endpoints as individual `- [ ]` items carrying method, path, purpose and the guard/role scope, ordered create → read → aggregates so they can be built one at a time; the pre-existing **Behavior** blocks are untouched. 136 endpoint items across the 15 modules, replacing the grouped `CRUD …` / `A / B` bullets that could not be ticked individually. Fixed along the way: `GET /api/v1/:role` became explicit `/teachers`, `/students`, `/parents` (which is what the frontend actually calls); `GET /schools/current`, `GET /fees/invoices` and `GET /fees/summary` were **added** because the frontend already consumes them but the PRD never defined them; §4.14's "Chart.js-ready" corrected to Recharts-ready (it contradicted the Recharts decision); Timetables (§4.8) gained an explicit post-MVP note, since no phase claimed it. `Phases.md` now cites the PRD sections per phase and carries the granular items, with a note on why OTP registration must stay in Phase 1. **Open question:** the full contract is 136 endpoints against the frontend's 24 mocked ones — the frontend phases need re-sequencing as the backend lands.
- 2026-09-23 — **Feature-wise endpoint audit** against `Database.md` (33 tables → module → phase) and the §2 role matrix. All 13 declared modules map to a §4 section, so nothing is orphaned. Fixes: the AI assistant is **7** features, not 8 — the `ai_feature` enum and the frontend's `AiFeature` type both list 7, so §1.1, `Architecture.md` and `Phases.md` were corrected (§9 was already right, and `Database.md` §18 open question 8 is now marked resolved); §2's AI row claimed Teacher ✅ generically while §4.12 scopes features per endpoint, so the matrix now names what each role actually gets; gaps filled — `GET /parents/:id/students` plus a parent's access to `GET /students/:id` (the `parent_students` table had no read path), `attendance:marked` promoted from Behavior to a checklist item, and §4.16 for `GET /health`, the one endpoint with no feature module; role markers realigned to §2 for exam create/publish and fee `create-order`. **Correction to my previous entry:** I added timetables to Phase 2, but `Database.md` tags those tables `phase: none` and the frontend phases call the builder post-MVP — they are now marked post-MVP in both `Phases.md` and §4.8, and `Database.md` §18 question 4 is marked resolved. 139 endpoint items now.
- 2026-09-23 — **PRD §4 synced to the `Database.md` schema, and update verbs corrected.** Every feature section now opens with a `**Tables:**` line naming the tables it owns (taken from `Database.md` §17 and the per-table `module:` tags), so an endpoint can be traced to its schema in one hop and the two documents can be diffed against each other. Method guidance: **`PATCH` for partial updates is now the default** — 13 update routes converted from `PUT`, since a settings/profile/edit body only ever carries the changed fields. The single remaining `PUT` is deliberate and documented: `PUT /timetables/:id` replaces the whole `periods` set, which is what `PUT` is for. The rule is written into `PRD.md` §5 (Methods) and `Design.md` (URL conventions) so it is contract, not preference. `frontend/src/services/apiClient.ts` gained a matching `patch()` helper — it only had `put()`, so the contract and the client had diverged before any update form was written.
- 2026-09-23 — `GET /dashboard/admin` payload extended: the frontend dashboard now shows an attendance trend beside the fee chart, so the summary carries a **per-day attendance series** (share present+late per register day, oldest first, `value` is a percentage where `collectionTrend`/`quarterlyTrend` carry paise). It also carries **class performance** (average marks per class as a percentage) and a **recent-activity feed** merged from payments, homework submissions and published notices — no audit table, so the endpoint derives it from rows that exist rather than inventing events. PRD §4.14's checklist line names all three. The alternative for attendance was a second round-trip to `/attendance/analytics`, which would have meant two loading states on one screen.
- 2026-09-23 — `GET /dashboard/admin` carries three more series for the new dashboard row: **upcoming exams** (subject papers dated today or later, soonest first, with a day countdown), **pending fees** (invoices with a balance left, most urgent first — `amount - discount - paid`, so never negative) and **calendar entries** (school events plus future exam days, sorted). Note the demo data now holds a future `Unit Test 2` per class: previously every exam sat 21 days in the past, so an "upcoming" list had nothing truthful to show. Marks follow the same rule as production should — `exam_results` only exist for papers whose start date has passed, so a scheduled test has no results yet.
- 2026-09-23 — `GET /students` read model extended for the new admin roster: each row now carries **`rollNo`** (position in the class), **`attendancePercentage`** (present+late share of the register) and **`feeStanding`** (initially `PAID`/`PENDING`, later widened to three states — see the profile entry below), and the endpoint accepts a **`feeStanding`** filter alongside class and search. The frontend computes all three in its mock adapter exactly as this list query should — roll number derived from class order, attendance from the register, standing from `amount - discount - paid` — so the component renders what it is handed instead of aggregating. PRD §4.3's checklist line names them.
- 2026-09-23 — §4.3 grew the details the admin enrolment form collects. `students` gains a **`roll_no`** column with a _partial_ unique index (`unique (class_id, roll_no) where deleted_at is null`) so a removed student's roll frees up without the old row blocking it; `parents` gains **`address`** (the guardian's email stays on `users`, where it already lived, so no duplicate column). `POST`/`PATCH /students` now take the roll — next free in the class when omitted, 409 `STUDENT_ROLL_TAKEN` when already used — and a **guardian block** (name, email, phone, address) that reuses an existing parent by email instead of creating a second record. `GET /students` rows carry the primary guardian's contact alongside the existing roll-ups. Database.md's table spec, keys/indexes, index plan and ERD were updated in the same pass, per the schema-stays-in-sync rule.
- 2026-09-23 — The student profile screen needed payloads the checklist already had endpoints for, so they were filled in rather than invented: `GET /students/:id` returns the roster row plus homeroom teacher, days present/absent and the current streak; `GET /attendance/student/:id` returns totals plus a per-month breakdown (present/absent/late/total/rate, with the rate counting present + late so it agrees with the roster); `GET /results/student/:studentId` returns one row per subject paper (exam, type, marks, total, %, grade, pass/fail, published) plus passed/failed/average; `GET /fees/history/:studentId` returns a paid/due/total summary and one row per invoice. One endpoint is new to the contract: **`GET /students/:id/documents`**, which returns an empty list until uploads exist — the profile's Documents tab renders its empty state from that, so no fake rows were needed. §4.3's checklist names both additions, and `feeStanding` is now three-state (**PAID / UNPAID / OVERDUE**), a balance past its due date counting as overdue even when the stored invoice status has not caught up.
- 2026-09-23 — Sync pass driven by the frontend profile screen. The mock's route params now match the checklist's names (`/attendance/student/:studentId`, `/results/student/:studentId`, `/fees/history/:studentId`), so code and PRD read the same. One schema gap surfaced: **`GET /students/:id/documents` has no backing table** — the Documents tab renders its empty state from a real but empty endpoint, so either a `student_documents` table lands before the upload flow or the endpoint goes; recorded as `Database.md` §18 question 14. A second gap worth knowing: the PRD says grades come from the school's grading scheme in `School.settings`, while the demo derives them from a fixed A+/A/B/C/D/F scale — acceptable for mock data, but the backend must read the scheme rather than reuse that scale.
- 2026-09-23 — Enrolment now sends an **invite**. `POST /students` provisions the login with a generated password and leaves it **unverified**; the password goes out by email with a verification link and is never returned to the client, and signing in before confirming is refused with 403 `AUTH_NOT_VERIFIED`. The mock implements the same shape — a `mockOnlyPassword` field carries it so the demo (which has no inbox) can still be exercised, and the login endpoint enforces the verification rule — while the real API must email it and omit that field. `otp_purpose` gained `INVITE` in §3 and in Database.md (§2 enum + the `otps` column), which is where the verification link's token belongs. PRD §4.3's endpoint line, its Behavior block and §4.15's mail triggers all describe the flow.
- 2026-09-23 — `students` gained a **`blood_group`** column (a `blood_group` enum: the eight standard groups, nullable so an import can land without it) — the admin enrolment form requires it alongside date of birth and gender, and the profile's Overview shows it. Database.md's §2 enum list and the table spec carry it; the API rejects a create without it. The frontend demo seeds every student deterministically across the eight groups rather than randomly, so the roster is stable between loads.

## Currently working on

- File: —
- Task: Phase 1 — NestJS scaffold, Supabase client module + base schema migration, school registration + OTP, JWT auth
- Blocker: none

## Decisions

- Stack locked: NestJS + Supabase (PostgreSQL) (replacing original MERN/Prisma plan)
- Payments: Stripe (international) + SSLCommerz (Bangladesh) — Razorpay removed
- Email: Resend (transactional API) — Nodemailer/SMTP removed
- Money stored as integer paise; dates ISO-8601; UUIDs for all IDs
- Multi-tenant via `schoolId` on every table + tenant guard — do not add unscoped queries
- API contract defined in `docs/backend/Design.md` — follow envelopes exactly
- Recharts on frontend (not Chart.js) — chart endpoints should return plain series data
- DB identifiers are `snake_case` (Postgres/Supabase idiom, no quoted identifiers in raw SQL); services map them to the camelCase JSON required by `Design.md`. The PascalCase/camelCase names in PRD §3 are logical model names, not table names — see `docs/backend/Database.md` §1.
- ERD reference: `docs/backend/Database.md` is the schema source of truth alongside `supabase/migrations/` — change both together.

# Memory — Backend

## Completed

- 2026-09-21 — Backend PRD written (NestJS + Supabase/PostgreSQL) in `docs/backend/PRD.md`
- 2026-09-21 — Docs scaffolded per vibe-coding structure (Architecture, Rules, Phases, Design, Memory)
- 2026-09-23 — Stack dependencies updated in `backend/package.json`: removed Prisma/Razorpay/Nodemailer; added Supabase, Stripe, SSLCommerz, Resend. Docs refreshed to match.
- 2026-09-23 — Added `docs/backend/Database.md` — the feature-wise ERD derived from PRD §3/§4. Table-by-table: a feature-level ERD per PRD feature module, then one section per table (heading + metadata tag + full column spec + **Keys** + **Indexes** + **Constraints** + its own ERD) for all 33 tables. Keys carry PK/UK and every FK with its `on delete` action; §13 is a consolidated Foreign Key Map (86 FKs) and §14 the Index Plan (every index, incl. the mandatory `school_id` index per tenant table). Also enums, transaction boundaries, a table inventory, PRD→table coverage, and 13 open questions (§18). 43 mermaid diagrams total. §19 adds a **full-schema ERD** — one themed diagram (§19) containing all 33 tables, all 86 FK edges and the 2 logical links, themed to Design.md tokens via a mermaid `%%{init}%%` directive. Linked from `AGENTS.md` and `backend/README.md`. No migrations exist yet — every table is a design target.

- 2026-09-23 — **PRD §4 rewritten as endpoint checklists, Phases expanded to match.** Every feature module now lists its endpoints as individual `- [ ]` items carrying method, path, purpose and the guard/role scope, ordered create → read → aggregates so they can be built one at a time; the pre-existing **Behavior** blocks are untouched. 136 endpoint items across the 15 modules, replacing the grouped `CRUD …` / `A / B` bullets that could not be ticked individually. Fixed along the way: `GET /api/v1/:role` became explicit `/teachers`, `/students`, `/parents` (which is what the frontend actually calls); `GET /schools/current`, `GET /fees/invoices` and `GET /fees/summary` were **added** because the frontend already consumes them but the PRD never defined them; §4.14's "Chart.js-ready" corrected to Recharts-ready (it contradicted the Recharts decision); Timetables (§4.8) gained an explicit post-MVP note, since no phase claimed it. `Phases.md` now cites the PRD sections per phase and carries the granular items, with a note on why OTP registration must stay in Phase 1. **Open question:** the full contract is 136 endpoints against the frontend's 24 mocked ones — the frontend phases need re-sequencing as the backend lands.
- 2026-09-23 — **Feature-wise endpoint audit** against `Database.md` (33 tables → module → phase) and the §2 role matrix. All 13 declared modules map to a §4 section, so nothing is orphaned. Fixes: the AI assistant is **7** features, not 8 — the `ai_feature` enum and the frontend's `AiFeature` type both list 7, so §1.1, `Architecture.md` and `Phases.md` were corrected (§9 was already right, and `Database.md` §18 open question 8 is now marked resolved); §2's AI row claimed Teacher ✅ generically while §4.12 scopes features per endpoint, so the matrix now names what each role actually gets; gaps filled — `GET /parents/:id/students` plus a parent's access to `GET /students/:id` (the `parent_students` table had no read path), `attendance:marked` promoted from Behavior to a checklist item, and §4.16 for `GET /health`, the one endpoint with no feature module; role markers realigned to §2 for exam create/publish and fee `create-order`. **Correction to my previous entry:** I added timetables to Phase 2, but `Database.md` tags those tables `phase: none` and the frontend phases call the builder post-MVP — they are now marked post-MVP in both `Phases.md` and §4.8, and `Database.md` §18 question 4 is marked resolved. 139 endpoint items now.
- 2026-09-23 — **PRD §4 synced to the `Database.md` schema, and update verbs corrected.** Every feature section now opens with a `**Tables:**` line naming the tables it owns (taken from `Database.md` §17 and the per-table `module:` tags), so an endpoint can be traced to its schema in one hop and the two documents can be diffed against each other. Method guidance: **`PATCH` for partial updates is now the default** — 13 update routes converted from `PUT`, since a settings/profile/edit body only ever carries the changed fields. The single remaining `PUT` is deliberate and documented: `PUT /timetables/:id` replaces the whole `periods` set, which is what `PUT` is for. The rule is written into `PRD.md` §5 (Methods) and `Design.md` (URL conventions) so it is contract, not preference. `frontend/src/services/apiClient.ts` gained a matching `patch()` helper — it only had `put()`, so the contract and the client had diverged before any update form was written.
- 2026-09-23 — `GET /dashboard/admin` payload extended: the frontend dashboard now shows an attendance trend beside the fee chart, so the summary carries a **per-day attendance series** (share present+late per register day, oldest first, `value` is a percentage where `collectionTrend`/`quarterlyTrend` carry paise). It also carries **class performance** (average marks per class as a percentage) and a **recent-activity feed** merged from payments, homework submissions and published notices — no audit table, so the endpoint derives it from rows that exist rather than inventing events. PRD §4.14's checklist line names all three. The alternative for attendance was a second round-trip to `/attendance/analytics`, which would have meant two loading states on one screen.
- 2026-09-23 — `GET /dashboard/admin` carries three more series for the new dashboard row: **upcoming exams** (subject papers dated today or later, soonest first, with a day countdown), **pending fees** (invoices with a balance left, most urgent first — `amount - discount - paid`, so never negative) and **calendar entries** (school events plus future exam days, sorted). Note the demo data now holds a future `Unit Test 2` per class: previously every exam sat 21 days in the past, so an "upcoming" list had nothing truthful to show. Marks follow the same rule as production should — `exam_results` only exist for papers whose start date has passed, so a scheduled test has no results yet.
- 2026-09-23 — `GET /students` read model extended for the new admin roster: each row now carries **`rollNo`** (position in the class), **`attendancePercentage`** (present+late share of the register) and **`feeStanding`** (initially `PAID`/`PENDING`, later widened to three states — see the profile entry below), and the endpoint accepts a **`feeStanding`** filter alongside class and search. The frontend computes all three in its mock adapter exactly as this list query should — roll number derived from class order, attendance from the register, standing from `amount - discount - paid` — so the component renders what it is handed instead of aggregating. PRD §4.3's checklist line names them.
- 2026-09-23 — §4.3 grew the details the admin enrolment form collects. `students` gains a **`roll_no`** column with a _partial_ unique index (`unique (class_id, roll_no) where deleted_at is null`) so a removed student's roll frees up without the old row blocking it; `parents` gains **`address`** (the guardian's email stays on `users`, where it already lived, so no duplicate column). `POST`/`PATCH /students` now take the roll — next free in the class when omitted, 409 `STUDENT_ROLL_TAKEN` when already used — and a **guardian block** (name, email, phone, address) that reuses an existing parent by email instead of creating a second record. `GET /students` rows carry the primary guardian's contact alongside the existing roll-ups. Database.md's table spec, keys/indexes, index plan and ERD were updated in the same pass, per the schema-stays-in-sync rule.
- 2026-09-23 — The student profile screen needed payloads the checklist already had endpoints for, so they were filled in rather than invented: `GET /students/:id` returns the roster row plus homeroom teacher, days present/absent and the current streak; `GET /attendance/student/:id` returns totals plus a per-month breakdown (present/absent/late/total/rate, with the rate counting present + late so it agrees with the roster); `GET /results/student/:studentId` returns one row per subject paper (exam, type, marks, total, %, grade, pass/fail, published) plus passed/failed/average; `GET /fees/history/:studentId` returns a paid/due/total summary and one row per invoice. One endpoint is new to the contract: **`GET /students/:id/documents`**, which returns an empty list until uploads exist — the profile's Documents tab renders its empty state from that, so no fake rows were needed. §4.3's checklist names both additions, and `feeStanding` is now three-state (**PAID / UNPAID / OVERDUE**), a balance past its due date counting as overdue even when the stored invoice status has not caught up.
- 2026-09-23 — Sync pass driven by the frontend profile screen. The mock's route params now match the checklist's names (`/attendance/student/:studentId`, `/results/student/:studentId`, `/fees/history/:studentId`), so code and PRD read the same. One schema gap surfaced: **`GET /students/:id/documents` has no backing table** — the Documents tab renders its empty state from a real but empty endpoint, so either a `student_documents` table lands before the upload flow or the endpoint goes; recorded as `Database.md` §18 question 14. A second gap worth knowing: the PRD says grades come from the school's grading scheme in `School.settings`, while the demo derives them from a fixed A+/A/B/C/D/F scale — acceptable for mock data, but the backend must read the scheme rather than reuse that scale.
- 2026-09-23 — Enrolment now sends an **invite**. `POST /students` provisions the login with a generated password and leaves it **unverified**; the password goes out by email with a verification link and is never returned to the client, and signing in before confirming is refused with 403 `AUTH_NOT_VERIFIED`. The mock implements the same shape — a `mockOnlyPassword` field carries it so the demo (which has no inbox) can still be exercised, and the login endpoint enforces the verification rule — while the real API must email it and omit that field. `otp_purpose` gained `INVITE` in §3 and in Database.md (§2 enum + the `otps` column), which is where the verification link's token belongs. PRD §4.3's endpoint line, its Behavior block and §4.15's mail triggers all describe the flow.
- 2026-09-23 — `students` gained a **`blood_group`** column (a `blood_group` enum: the eight standard groups, nullable so an import can land without it) — the admin enrolment form requires it alongside date of birth and gender, and the profile's Overview shows it. Database.md's §2 enum list and the table spec carry it; the API rejects a create without it. The frontend demo seeds every student deterministically across the eight groups rather than randomly, so the roster is stable between loads.
- 2026-09-23 — The invite loop now actually closes. A new account's password was previously random, which made an invited login unusable in the demo (nothing could read the toast's copy twice) — it is now the shared **demo password**, drawn from one `DEMO_PASSWORD` constant that the demo accounts and the login screen's note also read, so there is a single source for it. A random password remains what a real API must generate per invite. The missing other half was the confirm step: **`POST /auth/verify-invite`** now exists in PRD §4.2 and in the mock — it takes the address and the emailed one-time code (an OTP of purpose `INVITE`, per §3), flips the login to verified, and treats a repeat confirmation as success rather than an error. `resolveLogin` already refused an unconfirmed account with 403 `AUTH_NOT_VERIFIED`, so with both halves in place a new teacher or student can be signed in after confirming.
- 2026-09-23 — **Teachers module** (PRD §4.3) filled in. `teachers` gained **`subject`** and **`experience_years`** — profile attributes, distinct from the class-subject assignments in `subjects.teacher_id` — and a new **`teacher_classes`** join table records which classes a teacher takes, separate from `classes.class_teacher_id` (one homeroom teacher per class) so a teacher can teach in several without leading any. A `unique (teacher_id, class_id)` index keeps an assignment single, and `POST`/`PATCH /teachers` take the set wholesale (a `PATCH` replaces rather than merges). The write routes reuse the enrolment **invite** rule: the login is created unverified with a generated password and 409 `TEACHER_EMAIL_TAKEN` guards the address. Database.md carries all of it — the columns, the new section with keys/indexes/ERD, the FK map, the index plan, the coverage row, the table inventory (renumbered, 34 tables) and both ERDs (34 tables / 88 FKs). PRD §4.3's teacher lines describe the fields, the read model and the invite.
- 2026-09-24 — **Email templating switched to React Email.** `react-email` added to `backend/package.json`; the `mail/` module's messages are now `.tsx` React Email components in `src/mail/templates/`, rendered to HTML with `render()` (re-exported by `react-email`) and sent through Resend — replacing the earlier hand-written/Handlebars template plan (which was never installed). Docs swept in the same pass: `Architecture.md` (app flow, `mail/` tree line, tech stack + a React Email bullet), `Rules.md` (use/avoid), `PRD.md` (§1.2 stack row + §4.15 MailModule behavior — the stale Handlebars line), `Phases.md` (Phase 1 MailModule line), `CODE_REVIEWER.md` (§3 checklist) and `backend/README.md` (folder tree, stack, conventions). Open for the MailModule wiring: `.tsx` templates need `jsx` enabled in the backend `tsconfig` (it pre-dates any React), and `react`/`react-dom` — required peers of `react-email` — arrive transitively rather than being declared.
- 2026-09-24 — **§4.6 (Fees) expanded to the collect/report flows, and the schema caught up.** The frontend now ships the fee module the PRD only sketched, so the checklist was made to match what the screens actually call. New endpoints: `POST/PATCH/DELETE /fees/heads` (a head is its own resource — the Fee structure tab edits one at a time rather than replacing a whole structure), `POST /fees/invoices` (raise a single invoice from a head; 409 `FEE_INVOICE_EXISTS` on a duplicate, 400 when the head belongs to another class), `GET /fees/collect/summary|students|student/:studentId` (the Collect fee tab and its per-student page), `GET /fees/dashboard` (the two chart series + the defaulter list), `GET /fees/payments/:id/receipt`, and a four-route report family — `/fees/reports/day-book`, `/class`, `/defaulters`, `/student-ledger` — alongside the existing aggregate + CSV pair. `GET /fees/reports/day-book` **defaults to the most recent collection day** when no `date` is passed, so the screen opens populated instead of empty. Behaviour records three rules the implementation must keep: the receipt is **derived** (payment + invoice + student + head), the collect summary/dashboard/reports are **aggregates computed server-side** (the client never rolls up a list it fetched), and a manual payment may not exceed the invoice's outstanding balance. **Database.md moved in the same pass** (§1 format, metadata tags and per-table ERDs unchanged): `fee_heads` gained `due_date` + `description`; `fee_invoices` gained `fee_head_id` (FK → `fee_heads.id`, `set null` — the head titles the row, the structure still groups it) and `notes`; `fee_payments` gained `remarks` (cheque/DD reference); `concessions` gained `category`; `payment_method` gained `DEMAND_DRAFT` and `ONLINE`; and a `concession_category` enum (`SIBLING`, `MERIT`, `SC_ST`, `CUSTOM`, `STAFF_WARD`) was added. §2, §8 (four tables + two ERDs), §13 FK map (89 FKs now), §14 index plan (`(fee_head_id)`) and §19 (header count, edge and nodes) were all swept — nothing is one-sided. `Architecture.md`'s `fees/` line now names receipts and reports; `Design.md` gained the report-route-family convention. Frontend counterpart recorded in `docs/frontend/Memory.md`; the 19 mock routes were verified with a 29-case probe over the fee endpoints.

- 2026-09-24 — **§4.7 (Homework) synced to the frontend module, and `homework` gained `max_marks`.** The frontend now ships the homework grid and its assignment form, so the checklist was made to match what the screens actually call: `POST /homework` takes `title`, `description`, `dueDate` and an optional `maxMarks`, and `GET /homework` accepts `search`/`status` alongside `classId`/`subjectId`. Behavior records three rules the implementation must keep: a list row's `status` is **derived** (`OVERDUE` once `dueDate` has passed, `ACTIVE` otherwise), `maxMarks` is an optional ceiling (`null` = unmarked), and `DELETE /homework/:id` is a **soft delete** so submissions keep their parent. **Database.md moved in the same pass:** `homework` gained `max_marks integer null` with `check (max_marks is null or max_marks > 0)` — the §6 intro, table spec, per-table ERD and the §19 full-schema ERD all carry it. The FK map and index plan are unchanged: `max_marks` is neither a key nor a filter, so it takes no index. One contract detail worth keeping: a class change on its own must not leave an assignment holding a subject from the class it just left — the PATCH validates the **resulting** `(classId, subjectId)` pair before writing either field.

- 2026-09-24 — **§4.8 (Timetable) synced to the frontend module, and `periods` gained `label`.** The frontend now ships the weekly timetable against the mock API (class picker, subject legend, day × period grid, per-cell editing, class-wide period rows, stat tiles), so the checklist was written to match what the screens call. Three endpoints were added: `PATCH /timetables/class/:classId/slots` (set or clear one cell — the contract's `PUT /timetables/:id` replaces a whole day's set, which cannot express a single-cell edit), `POST /timetables/class/:classId/periods` and `DELETE /timetables/class/:classId/periods/:orderIndex`. All three hang off the class and address a row by `orderIndex`, because the week's rows are one structure stored per day (see below). **Database.md moved in the same pass:** `periods` gained `label text null` — named rows ("Short Break", "Lunch Break", "Assembly") need a stored name, and a null label on a teaching row is exposed as `Period n` counted over teaching rows only, which is what keeps the period after a break numbered 4 rather than 5. The §7 intro, the `timetables` intro, the table spec, the per-table ERD and the §19 full-schema ERD all carry it; the FK map and index plan are unchanged (`label` is neither a key nor a filter). §18 question 4 was updated: `phase: none` no longer means "nothing exists", since the frontend module ships on mock data — it means the backend endpoints are still unscheduled. **Known gap, recorded rather than enforced:** the contract's "conflict detection: teacher double-booking" is _not_ modelled by the mock, because this demo school cannot satisfy it — 20 classes run 6 teaching rows on 6 days but only 8 teachers exist, so any slot needs 20 distinct teachers. The seeded week double-books by construction; a real backend with a fuller staff list must enforce it.

- 2026-09-24 — **§4.11 (Notices) synced to the frontend notice-board module, and `notices` gained `priority` + `author_name`.** The frontend now ships the board (priority-tinted cards, search, pagination) plus an admin create/edit/delete form, so the checklist was written to match what the screens call: `POST /notices` takes `title`, `body`, `priority` and an optional `authorName` (a blank byline falls back to the publisher's display name), `PATCH` edits and publishes a draft, and `DELETE` is a soft delete. **Database.md moved in the same pass:** `priority notice_priority not null default 'MEDIUM'` — a new `notice_priority` enum (`HIGH`, `MEDIUM`, `LOW`) added to §2 — and `author_name text not null`, the byline the frontend already read off the seeded rows while the schema only had `published_by_id`. The §11 intro, the table spec, the per-table ERD and the §19 full-schema ERD all carry them; the FK map and index plan are unchanged (`priority`/`author_name` are neither keys nor filters). Behaviour records that the board form has **no draft state** — a notice is created published and editing a draft publishes it — and that `priority` tints the card without reordering the feed, which stays newest-first.

- 2026-09-24 — **§4.10 (Chat) completed for the frontend's communication module.** The tables and both read endpoints already existed, so this pass filled the gaps a consumer needs: the two GETs now name their **read models** — the _inbox row_ (the other participant's name plus a class/subject/role byline, the newest message, `lastMessageAt`, and a per-caller `unreadCount`) and the _history row_ (`mine`/`sentAt`/`read`) — and the Behavior block records that a read stamps **both** `messages.read_at` (the sender's double-check) and `conversation_participants.last_read_at` (the caller's unread marker), that both reads are caller-scoped with a **403** for a non-participant, and §3's Conversation bullet now carries `lastReadAt`. `Database.md` §10 needed **no new columns** — `conversations.last_message_at`, `conversation_participants.last_read_at` and `messages.read_at` already support the read models — but the two per-table notes were untangled so the receipt and the unread marker are not confused. `Architecture.md` gained a **Real-time chat** section: the gateway-vs-REST split, persist-then-fan-out, read state, caller scoping and the shared DTO shapes. No migration is implied — the schema is unchanged.

- 2026-09-24 — **§4.2 gained Roles & permissions, with two new tables and a `permission_group` enum.** The frontend now ships the per-teacher permission editor, so the contract was written to match what it calls: `GET /permissions` (the catalogue, grouped and ordered), `GET /permissions/staff` (the picker with each active teacher's grant count), and `GET`/`PUT /users/:userId/permissions` — a `PUT` because the body carries the **whole set**, which is what lets an admin turn a role default off. **Schema:** `permissions` (the platform-wide catalogue — `key` (`resource.action`, CHECK-constrained), `group`, `label`, `sort_order`) and `user_permissions` (the account's effective set: `school_id`, `user_id`, `permission_id`, `granted_by_id`, `unique (user_id, permission_id)`), plus the `permission_group` enum in §2. **`permissions` is the first table with no `school_id`** — it is shared by every school, so it now appears among §16's tenancy exceptions, and §18 q15 records the decision that role defaults stay in the PRD §2 matrix (applied at account creation) rather than in a `role_permissions` table. The authorization chain is documented as `@Roles()`/`RolesGuard` then `@RequirePermission()`/`PermissionsGuard` (PRD §4.2 Behavior, `Rules.md`, `Architecture.md`). **Database.md moved in the same pass:** §3 spec + both ERDs, §13 FK map (4 rows → 93 FKs), §14 index plan, §15 inventory (34 → 36 tables), §16, §17 coverage, §18 (q15) and §19 (36 tables / 93 FKs / `schools` fans out to 32). No migration is implied beyond the two new tables.

- 2026-09-24 — **§4.9 (Exams) synced to the new Tests & exams module, and the schema learned what a "test" is.** The frontend now ships the three-tab screen (Tests / Exams / Results), so the contract was written to match what it calls. **The modelling decision: tests and exams share `exams`, told apart by a new `exam_kind` enum (`TEST`/`EXAM`)** — a test is one subject sat on one date, an exam is a multi-subject window, and only an exam produces report cards. That keeps `results` a single table (no polymorphic FK), which is what lets the Results tab enter marks for either with one code path. Also added: **`ANNUAL`** to `exam_type`; `exam_subjects.duration_min integer not null default 60` (each paper carries its own duration, shown in the schedule and set in the form); `exams.description`; and `results.remarks` (the teacher's note beside a mark). `exams`' index became **`(school_id, kind, class_id, start_date)`** because `kind` is now a filter — the one index-plan change. **Endpoints:** new `GET /exams/:id/results` (the sheet: every subject with its entry count, the class roster and the entries so far, so all subjects load together for bulk entry); `GET /exams` filters on `kind`/`subjectId`; `POST /exams` takes the shape its kind needs; `PATCH` replaces the subject set whole and 409s once published; `POST /exams/:id/marks` upserts the grid's cells and refuses a mark above the paper's total. Behaviour records the derived `status` (`COMPLETED`/`UPCOMING`), the per-paper ceiling and the meaning of `remarks`. Database.md carried all of it (§2 both enums, §9 intro/spec/ERDs, §14 index plan); the FK map, inventory and §19 counts are unchanged because no table was added.

- 2026-09-24 — **Timetable scoped by role: one read (`GET /timetables/me`) instead of the client choosing.** The frontend now shows each role only its own week, so the contract gained a self-scoped read and §4.8 records the rule: a teacher's is **their own lessons on the same days × periods grid** — the period rows are shared by every class, so each cell names the class they are in and a blank means they are free — while a student's is their class and a parent's is their child's class. The class routes (`GET /timetables/class/:classId`, `PATCH …/slots`, `POST`/`DELETE …/periods`) stay the **admin's**, which is what the picker and the editor drive; an admin has no personal week (the endpoint 404s for them rather than guessing). No schema change — `periods.teacher_id` already carries what a teacher week needs, so this is a read-model decision only.

- 2026-09-24 — **A guardian's children on the timetable, and the two logins an enrolment creates.** `GET /timetables/me` now takes an optional `?studentId=` and returns a `children[]` list for a guardian, who can switch between **their own** children (any other id is a 403, and a student cannot open another student's week either) — the same class grid they already got, just addressed per child. Separately, the enrolment email rule was made explicit in both documents: the email the form collects **is** the login (never a generated address), and an enrolment provisions **two** unverified accounts — the student's own email and the guardian's — each emailed its password and invite, so the password that arrives is the one the account holds. `PRD.md` §4.3 records it (`POST`/`PATCH /students` take/re-check the email, 409 `STUDENT_EMAIL_TAKEN`), and `Database.md` §3 gained the **Logins** reminder, including that no profile table carries an email column — `students`, `teachers` and `parents` all read it from `users`. Found while testing the flow: the mock had left `/auth/verify-invite` out of its public paths, so an invite could never be confirmed without a token even though `PRD.md` §4.2 marks it `(public)`; fixed.

- 2026-09-24 — **§4.12 (AI Assistant) synced to the assistant screen, and `ai_conversations` gained `prompt_args`.** The frontend now ships the tool screen — a rail of four tools (quiz, homework helper, event planner, notice) with its own form per tool — so the contract was written to name what each call takes and returns, and `GET /ai/conversations` was given an explicit **generation read model** (`id`, `feature`, `title`, `promptArgs`, `output`, `createdAt`, newest first) rather than "the caller's history": that is what the panel renders, and it is why the schema needed a column. **`ai_conversations.prompt_args jsonb not null default '{}'`** holds the tool form's own fields, so a past generation can be reopened with its inputs and the prompt template re-run — the messages array only carries the rendered turns, not the arguments behind them. The §12 intro, the table spec, the per-table ERD and the §19 full-schema ERD all carry it; the FK map, index plan and inventory are unchanged (`prompt_args` is neither a key nor a filter). Behaviour records that four of the seven features own a screen and that each is offered only to the roles §2 allows (teacher → quiz; student → quiz + homework helper; guardian → none).

## Currently working on

- File: —
- Task: Phase 1 — NestJS scaffold, Supabase client module + base schema migration, school registration + OTP, JWT auth
- Blocker: none

## Decisions

- Stack locked: NestJS + Supabase (PostgreSQL) (replacing original MERN/Prisma plan)
- Payments: Stripe (international) + SSLCommerz (Bangladesh) — Razorpay removed
- Email: Resend (transactional API) — Nodemailer/SMTP removed
- Email templates: React Email (`react-email`) `.tsx` components rendered to HTML with `render()`, sent via Resend — Handlebars/raw-HTML string templating dropped
- Money stored as integer paise; dates ISO-8601; UUIDs for all IDs
- Multi-tenant via `schoolId` on every table + tenant guard — do not add unscoped queries
- API contract defined in `docs/backend/Design.md` — follow envelopes exactly
- Recharts on frontend (not Chart.js) — chart endpoints should return plain series data
- DB identifiers are `snake_case` (Postgres/Supabase idiom, no quoted identifiers in raw SQL); services map them to the camelCase JSON required by `Design.md`. The PascalCase/camelCase names in PRD §3 are logical model names, not table names — see `docs/backend/Database.md` §1.
- ERD reference: `docs/backend/Database.md` is the schema source of truth alongside `supabase/migrations/` — change both together.
- `homework.max_marks` is a **nullable integer ceiling** for a graded submission (`null` = the task is unmarked), added when the frontend's assignment form landed. Grading itself still records a free-text `grade` + `remarks` on `homework_submissions` — `max_marks` only bounds what that grade is out of.
- Timetable period rows are **stored per day** (`periods` under each day's `timetables` row) but **managed class-wide** — a row is added to or removed from every day in one transaction and addressed by its `orderIndex`. That keeps the days from drifting apart without adding a class-level period table to the schema. `periods.label` names a row; a null label on a teaching row derives `Period n` over teaching rows only.
- `notices.priority` is a new `notice_priority` enum (`HIGH`/`MEDIUM`/`LOW`, default `MEDIUM`) that tints the board card; it deliberately does not reorder the feed. `notices.author_name` stores the byline the board form collects (blank falls back to the publisher's display name) — the frontend seed already carried `authorName` as a read-model field with no column behind it. Notices have no draft state in the UI: create publishes, and editing a draft publishes it.

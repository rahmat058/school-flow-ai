# Memory — Frontend

## Completed

- 2026-09-21 — Project scaffold: Vite + React 19 + TS + Tailwind 4, dashboard shell components (AppShell, Header, Sidebar, StatCard, StatGrid, RevenueChart, TransactionsTable, InsightsCard) with mock data in `src/data/dashboard.ts`
- 2026-09-21 — Docs scaffolded per vibe-coding structure (PRD, Architecture, Rules, Phases, Design, Memory)
- 2026-09-23 — Added frontend dependencies: zustand, framer-motion, date-fns, @react-pdf/renderer. Docs (Architecture tech stack, Memory decisions) updated to record them.
- 2026-09-23 — Adopted TanStack Query 5 (`@tanstack/react-query`) for all server state. Docs updated: Architecture (tech stack + data layer), Rules (Use/Avoid), CODE_REVIEWER (frontend checklist), this file. Not yet wired in `src/` — `QueryClientProvider` and the first query hooks land with the Phase 1 API client.
- 2026-09-23 — Added `motion` v13 for animations; docs updated (Architecture tech stack, Rules). Not yet used in `src/`. `framer-motion` remains a direct dependency but is redundant — `motion` depends on it, and `framer-motion` 13.4.x is the same library under its legacy name.
- 2026-09-23 — Built the design-system primitives in `src/components/ui/`: Input, Textarea, Select, Checkbox, Radio, Switch, Card, Modal + ConfirmDialog, Tabs, Table, DataTable, Pagination, Dropdown, Tooltip, Alert, Progress, Avatar, Skeleton, Spinner, EmptyState, Accordion. Raw HTML + Tailwind + `motion` only — no new packages. `Button` gained the `destructive` variant from Design.md; added `src/hooks/useControllableState.ts` and `MotionConfig reducedMotion="user"` in `main.tsx`. Not yet consumed by any page.
- 2026-09-23 — Fixed the TypeScript 6 config and unblocked `npm run build`: removed the deprecated `baseUrl` from `tsconfig.app.json` and made the mapping relative (`"@/*": ["./src/*"]`), which is what TS 6 requires once `baseUrl` is gone. `tsc -b && vite build` passes again (Vercel deploys were failing). That also exposed two real type errors that had been invisible — `HTMLHeadingElement` wrongly imported from `react` in `Card.tsx` (it is a DOM global) and a callback-variance mismatch in `Radio.tsx` — both fixed.
- 2026-09-23 — Root cause of the above: `npm run typecheck` ran `tsc --noEmit` against the solution-style root `tsconfig.json` (`"files": []`), which compiled **zero files**. Changed the script to `tsc -b` so it typechecks both referenced projects. Any earlier "typecheck clean" result was meaningless. Verified with `tsc -b -v` (builds `tsconfig.app.json` + `tsconfig.node.json`) and no files are emitted (`noEmit: true`).
- 2026-09-23 — Added `Toast` to `src/components/ui/` with `success` / `warning` / `error` / `info` tones (reusing the Alert palette: `bg-*-soft` + `border-*/20`), plus `ToastProvider` + `useToast`. The hook lives in `src/hooks/useToast.ts` rather than beside the component, because `react-refresh/only-export-components` rejects a file that exports both components and hooks — this keeps the documented `hooks/` convention. Provider is mounted in `main.tsx` **inside** `MotionConfig` so toasts respect reduced-motion. Motion: spring entry, `layout` reflow when one is dismissed, slide-and-fade exit through `AnimatePresence`; hovering a toast pauses its auto-dismiss (`duration: 0` makes it sticky). Accessibility: the region is always mounted (so the live region exists before content arrives) with `role="alert"` for error/warning and `role="status"` otherwise. Verified: `tsc -b`, eslint, a 9-case SSR smoke test (all four tones, provider mount, empty initial queue, context shape, `useToast` throwing outside a provider) and a built-CSS check that every utility compiles. **Not** verified automatically: auto-dismiss timing, hover-pause, exit animation and stacking — no DOM test environment is installed (no jsdom/happy-dom) and adding one needs approval.
- 2026-09-23 — Implemented **Phase 1** (auth, routing, API layer) and a list-page showcase: axios client (`src/services/apiClient.ts`) with JWT attach, envelope unwrapping and a normalised `ApiError`; mock adapter (`src/services/mockAdapter.ts`) serving every endpoint from `src/data/`; Zustand auth session with refresh-cookie bootstrap (`src/store/auth.ts`); public/private route groups with `ProtectedRoute`, `PublicOnlyRoute` and `RoleGuard`; login, school signup, OTP verification, forgot-password and reset-password screens; role-aware sidebar/header; and a school-data dashboard plus Students, Invoices and Notices list pages on TanStack Query. 19 demo data files mirror `docs/backend/Database.md`; the "Curator Pro" template data and its three dashboard components were replaced.
- 2026-09-23 — Converted all five auth forms (login, signup, OTP verification, forgot password, reset password) to **react-hook-form** 7.88: field state and validation moved from `useState` + hand-written validators to `register()` rules with `mode: 'onTouched'`; API failures surface through `setError('root.serverError')` instead of a separate error state; submissions use `mutateAsync` so `formState.isSubmitting` drives the button spinner. `Input` and `Textarea` now extend `ComponentPropsWithRef` so `register()`'s ref reaches the DOM node — without it react-hook-form cannot focus the first invalid field. Verified: `tsc -b`, eslint, prettier, `vite build`, and a 10-case smoke test asserting every field renders with its `name` attribute (proof it is bound) plus the unchanged guard decisions.
- 2026-09-23 — Display currency changed from ₹ (INR) to **$ (USD)**: `formatPaise`/`formatPaiseCompact` now format `en-US`/USD with M/k magnitudes instead of lakh/crore, `SchoolSettings.currency` is `'USD'`, and the seeded fee heads were rescaled to plausible dollar amounts (tuition $450/month, transport $120). Brand artwork moved into `src/lib/brand.ts`; the sidebar and the auth screens now show the real logos. The sidebar became a role-aware rail: overlay drawer below `lg`, and a 240px ⇄ 76px icon rail on desktop toggled from the header.
- 2026-09-23 — Sidebar toggle details: the rail width animates (`transition-[width,transform]`, 200ms ease-out) and labels collapse via `max-width`/`opacity` rather than unmounting, so icon-only links keep an accessible name; each nav link's `Tooltip` is enabled only while collapsed (`Tooltip` gained a `disabled` prop). The header toggle crossfades `PanelLeftClose` → `PanelLeftOpen` through `AnimatePresence`. Verified: a 6-case smoke test (USD formatting, both logo assets, rail width classes in both modes) and a built-CSS check that every rail/label utility compiles (`lg:w-[76px]`, `lg:max-w-0`, `transition-[max-width,opacity]`, …). Nav labels themselves cannot be asserted in a static render — the store serves its initial state — so they are verified by class emission only.
- 2026-09-23 — Two layout/affordance fixes. (1) **Pointer cursor:** Tailwind 4's preflight leaves `<button>` on the browser default cursor, so a base-layer rule in `src/styles/index.css` now sets `cursor: pointer` for `button`, `[role="button"]`, `summary`, `label[for]` and `a`, plus `cursor: not-allowed` for disabled / `aria-disabled="true"` controls. (2) **Sidebar scrolling:** the desktop rail is now `lg:sticky lg:top-0 lg:h-screen`, so it stays pinned while only the main column scrolls — previously the static sidebar stretched with the content and the account block ended up at the bottom of the page. The nav keeps `overflow-y-auto overscroll-contain` as the only internal scroller, for viewports shorter than the 12 nav items. Verified in the built CSS: the cursor rule is emitted after the preflight (`cursor:default` appears nowhere), and `position:sticky` / `height:100vh` / `overscroll-behavior:contain` all compile.
- 2026-09-23 — Auth wordmark looked too small: the cause was the **asset**, not the CSS. `public/school-flow.png` is a 2172×724 canvas whose artwork only spans 1979×430, so ~40% of the file height is transparent padding and `h-10 w-auto` rendered the logo at just 109×24px. Sized it to the artwork's own 4.6:1 ratio with `object-cover` (`h-14 w-65` = 56×260px), which crops the padding and renders the wordmark ~237×33px. Measured with a PNG alpha-bounding-box probe; dimensions and the sizing rule are recorded in `src/lib/brand.ts`. The real fix is to trim the PNG (or switch to SVG): the file is 416 KB, and `mobile-logo.png` is 1.7 MB at 1254×1254 **with no alpha channel**, so it cannot be used as a cropped mark.
- 2026-09-23 — Replaced the sidebar's logo image with a lucide `School` icon chip (`bg-primary-soft` / `text-primary`, `size-9` chip with an 18px glyph) — the raster mark was hard to read at 36px. `BRAND.mark` was deleted, so `public/mobile-logo.png` is now referenced nowhere (see `src/lib/brand.ts`; delete it or repurpose it as the app icon). `BRAND.wordmark` is still used by the auth screens.
- 2026-09-23 — Extracted the main-content container into one place: a Tailwind 4 `@utility page-container` in `styles/index.css` (centred, `max-width: 80rem` per Design.md, `padding-inline: 1rem` → `2rem` from `lg`). `AppShell` and `AuthShell` now use it instead of their own `mx-auto max-w-7xl px-4 lg:px-8` and `px-4` declarations, so width and padding are declared once. Pages stay container-free on purpose — the shell remains the single owner, which is what keeps padding from being applied twice; the remaining `mx-auto max-w-*` in the codebase are content caps (the auth grid, a paragraph), not containers. Verified in the built CSS: `.page-container{width:100%;max-width:80rem;margin-inline:auto;padding-inline:1rem}` plus the `lg` override.
- 2026-09-23 — Added a password visibility toggle. `Input` gained a `trailing` slot (adornment centred on the field, input gets `pr-11` so text clears it) and a new `components/ui/PasswordInput` wraps it with an `Eye`/`EyeOff` button carrying `aria-label` + `aria-pressed` so the state is announced. Applied to all four password fields — login, signup, and both reset-password fields — replacing `type="password"` on a bare `Input`; masked by default. Verified with a 4-case render check: one toggle per field, masked initially, `pr-11` present, and nothing rendering as `type="text"`.
- 2026-09-23 — Unified interactive hover on `bg-primary-soft`: replaced all 15 `hover:bg-canvas` occurrences (buttons, nav items, icon buttons, rows, pagination, modal/accordion triggers, the password toggle) plus the two `bg-canvas` "highlighted" states that mouse-enter drives (Dropdown menu items, Select options), 17 replacements across 11 files. Hovering a nav item now previews the active state, which is the same tint. Left deliberately different: solid fills darken (`hover:bg-primary-hover`, `hover:bg-error/90`), Dropdown's danger item keeps `hover:bg-error-soft`, and `DataTable` keeps `hover:bg-transparent` on header/skeleton rows. Verified in the built CSS — `.hover\:bg-primary-soft:hover` emitted, `.hover\:bg-canvas:hover` gone entirely, with `tsc -b`, eslint, prettier and `vite build` all passing.
- 2026-09-23 — Extended the hover treatment to text: `hover:text-ink` → `hover:text-primary` across the app, and the two mouse-driven menu lists now turn primary on hover **and** on keyboard highlight (Dropdown items, Select options). Dropdown's danger item stays `text-error` and is excluded from the primary highlight (a destructive row must not look like a normal one). Table rows deliberately keep ink text — the tint alone marks them, since they are data rather than controls. Accordion triggers and the secondary Button variant were the two remaining interactive hovers without a text change; both now carry `hover:text-primary` too.
- 2026-09-23 — Fixed the auth layout alignment: the demo panel was top-aligned to the grid row the **brand** occupied, so it floated above the form card instead of lining up with it. The brand now owns grid row 1, and the card and panel share row 2 (`lg:col-start-1` / `lg:col-start-2` + `lg:row-start-2`), so both start on the same line with the panel keeping its natural height (`lg:self-start`). `gap-10` split into `gap-x-10 gap-y-6` so the row gap replaces the brand's old `mb-6`; mobile order and spacing are unchanged. Verified with a 3-case render check and the built CSS (`.lg\:row-start-2{grid-row-start:2}`, `.lg\:col-start-2{grid-column-start:2}`, `row-gap` + `column-gap`).
- 2026-09-23 — Thin scrollbars, defined once in `styles/index.css`. WebKit/Blink get a 4px rounded pill (`12px` bar, `4px` transparent border, `background-clip: content-box`) on a transparent track; Firefox gets `scrollbar-width: thin` + `scrollbar-color`. **The standard properties are deliberately inside `@supports (-moz-appearance: none)`**: Chrome ignores `::-webkit-scrollbar` entirely once `scrollbar-width` is set, so setting it globally would have silently killed the pill styling in every Chromium browser — verified in the built CSS that `scrollbar-width` is declared exactly once and only in that block. Tokens `--color-scrollbar-thumb` / `-hover` added to `@theme` (neutral ink alphas, not indigo — scrollbars are chrome, not controls).

**Known issue (not fixed):** `vite.config.ts` uses `__dirname`, which the planned Vite `configLoader: 'native'` rejects — the build warns to use `import.meta.dirname`. Warning only today; will break on a future Vite major.

## Currently working on

- File: `src/routes/privateRoutes.tsx` · next: `src/pages/admin/StudentsPage.tsx`
- Task: Phase 2 — Admin core (students/teachers/parents management against the live API)
- Blocker: backend API not yet implemented — the frontend runs entirely on the mock adapter

## Decisions

- Backend is NestJS + PostgreSQL via Supabase — all API calls target `/api/v1`
- Recharts (not Chart.js) for charts — already installed
- State: Zustand for client-only global state (auth/session, school context) — no Redux
- Server state: TanStack Query 5 for all `/api/v1` data — query/mutation hooks in feature `api.ts` with cache + invalidation; never cache API responses in Zustand
- Animations: `motion` v13 for UI transitions — React APIs imported from `motion/react`; `framer-motion` is the legacy package name for the same library and must not be imported directly
- Dates: date-fns for formatting and academic-calendar helpers
- Reports: @react-pdf/renderer for PDF export (report cards, invoices)
- Mock data in `src/data/` is temporary; replace with API calls per phase
- Design tokens follow `docs/frontend/Design.md` — indigo primary, 4px grid
- UI primitives are hand-rolled from raw HTML + Tailwind in `src/components/ui/` — no component library (no Radix/Headless UI/shadcn). `motion` (`motion/react`) provides all animation, and `MotionConfig reducedMotion="user"` in `main.tsx` makes it respect OS reduced-motion settings.
- Auth session lives in a **Zustand** store (`src/store/auth.ts`) — access token in memory only, restored on reload via `POST /auth/refresh`. `Architecture.md`'s old `context/` entry was replaced by `store/`.
- Demo data is served **through** axios via a custom adapter, not around it: hooks, interceptors, JWT attach and error envelopes are exercised exactly as they will be against the backend. `VITE_ENABLE_MOCKS=false` switches to the real API with no other change.
- Route structure: `src/routes/paths.ts` is the single source of route strings; `publicRoutes.tsx` / `privateRoutes.tsx` are plain `RouteObject[]` composed by `AppRoutes`; pages are `React.lazy` so each ships as its own chunk.
- Guard _decisions_ are pure functions in `routes/guards/guardDecision.ts` (testable without a DOM — React Router's `<Navigate>` is a no-op on a static render, and zustand v5 serves its initial state to server renders).
- Demo logins: `admin@` / teacher / student / parent accounts at `brightfuture.edu`, password `demo1234`; the signup OTP is `123456`. Both are shown on the login screen while mocks are on.
- Forms use **react-hook-form** for state and validation. The `components/ui` input primitives must accept a ref (`ComponentPropsWithRef`) so `register()` works; select/checkbox/radio/switch are non-native and need `Controller` when a form uses them (no form does yet).
- **Debt:** no DOM test runner is installed, so interaction (submit, redirect, pagination clicks, the sidebar toggle itself) is unverified by automation. Verification to date: `tsc -b`, eslint, prettier, `vite build`, plus Node smoke tests over the axios/mock layer (11 cases), the guard decisions, the auth-page rendering (13 cases), react-hook-form field binding (10 cases) and the currency/brand/rail classes (6 cases).
- **Debt:** the `*Paise` names (types, fields, `formatPaise`) still describe the minor unit as paise while the UI now displays USD, and `docs/backend/Design.md` still defines money as "integer paise" with a ₹ example. Either rename to cents and update the backend contract, or keep paise as the storage unit and treat USD as display-only — needs a decision.

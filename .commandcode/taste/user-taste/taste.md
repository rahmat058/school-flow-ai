# User Taste

- Works on Windows; projects live on the Desktop (e.g. `C:\Users\USER\Desktop\<project>`). Confidence: 0.85
- Treats `template-management-app` (on Desktop) as the canonical reference project — explicitly wants new projects configured the same way as this template rather than from scratch. Confidence: 0.85
- Standard project tooling: husky + commitlint + commitizen (cz) + lint-staged + prettier, configured at the repo root. Confidence: 0.8
- Monorepo layout with `frontend/` and `backend/` folders; root package.json holds shared tooling and delegates to packages via `npm --prefix <pkg> run <script>`. Confidence: 0.7
- Conventional Commits with gitmoji-style type emoji prefixes in the header (✨ feat, 🐛 fix, 📚 docs, etc.), enforced by commitlint with a custom parser preset plus commitizen interactive prompts. Confidence: 0.75
- Husky hook setup: `commit-msg` (auto-prepend type emoji, then commitlint), `pre-commit` (lint-staged), `pre-push` (lint + format:check). Confidence: 0.75
- Treats husky `pre-push` validation (lint + format/typecheck) as a hard gate — will not `--no-verify` past a failure; surfaces the error and asks to fix it at the root (e.g. aligning dependency versions such as TypeScript with the `typescript-eslint`-supported range, and doing a clean `node_modules`/`package-lock.json` reinstall when the tree is in a partial state) rather than disabling or weakening the hook. Confidence: 0.75
- Prettier: no semicolons, single quotes, trailing commas "all", printWidth 120, 2-space indent, LF line endings. Confidence: 0.7
- ESLint flat config (`eslint.config.*`) per package; keep framework-appropriate configs (Vite vs Next) rather than copying blindly. Confidence: 0.6
- User's shell has `NODE_ENV=production` set globally — npm silently skips devDependencies, so clear NODE_ENV before `npm install`. Confidence: 0.9
- Prefers a single root-level `.gitignore` in monorepos that covers all packages (root, `frontend/`, `backend/`) using `**/` globs, rather than per-package gitignores; should also ignore build output, `*.tsbuildinfo`, `.env*` (except `.env.example`), and OS junk. Confidence: 0.6
- Communicates in terse, fragmentary instructions and expects the agent to infer the full intent and complete the task with sensible defaults without needing clarification — reinforces this by declining the agent's clarifying question and leaving it to proceed on its own reading, and by one-line unpunctuated feature asks that state only the desired outcome ("when subject add there should be form add a assign class where all class exists in dropdown ..."). Confidence: 0.8
- Active project `school-flow-ai` (on Desktop): a School Management System with multi-role dashboards — admin, teacher, student, parent; React frontend. Backend stack pivoted from MERN (MongoDB/Express) to NestJS + PostgreSQL on Supabase. Confidence: 0.8
- Backend framework preference: NestJS (TypeScript), replacing the original Express.js choice for `school-flow-ai`. Confidence: 0.9
- Database preference: PostgreSQL hosted on Supabase, accessed via the Supabase client (`@supabase/supabase-js`) as the data-access layer — user explicitly calls Supabase his "backend database orm" and had Prisma fully removed (dependencies, scripts, and docs). Confidence: 0.9
- Keeps project documentation in a `docs/` folder at the repo root, organized into per-package subfolders matching the monorepo layout (e.g. `docs/frontend/`, `docs/backend/`), and wants requirement docs (PRDs) structured feature-wise — one section per feature module rather than a flat spec. Confidence: 0.75
- Backend docs follow a per-concern file split and he expects each concern updated in its designated file: API routes/endpoints documented inside the feature's PRD section (`PRD.md`), the schema in `Database.md`, and cross-cutting design in `Architecture.md` (he names exactly these three when asking for feature docs). Confidence: 0.55
- Adopted the "vibe coding" documentation convention (from kazi-rahamatullah.com blog): six mandatory markdown files per package — PRD.md, Architecture.md, Rules.md, Phases.md, Design.md, Memory.md — maintained in each docs subfolder; Rules.md includes explicit AI boundaries and Phases.md uses "Done when" criteria per phase. Confidence: 0.8
- Shares images — both external reference material (links to blog posts, a reference ERD/screenshot to imitate) and screenshots of his own running app when reporting a visual defect — and expects the agent to actually look at them and act: apply the referenced style/structure, or diagnose the real root cause of the defect (e.g. the asset, not the CSS) rather than just nudging styles. For a new module he drops a whole set of per-screen reference mockups (ten images covering the reports module's four tabs, each screen pointed at its own image in the request) and expects each screen built to match its reference tab by tab — repeated for the settings module (a four-image set, one mockup per tab: School Profile, Academic, Notifications, Security, each image called out in the request as that tab's design). Confidence: 0.7
- Wants a root-level `AGENTS.md` as the single entry point for AI agent sessions, kept in sync with the `docs/` structure — it links every docs file with its purpose, states core agent rules (minimal scope, phase order, no unapproved deps/commits, validation before done), cross-package contracts, and quick commands. Confidence: 0.75
- Wants a `docs/CODE_REVIEWER.md` checklist that the agent applies to every diff before marking work done — sections for correctness, security (blocking), per-stack conventions (NestJS/Prisma backend, React/Tailwind frontend), performance, and scope hygiene, with severity-labeled findings (🔴 blocker / 🟡 should-fix / 🔵 nit). Confidence: 0.7
- Commits and tracks `package-lock.json` in every package directory (root, `frontend/`, `backend/`) and ensures lock files are not gitignored — reproducible installs across the monorepo. Confidence: 0.85
- Frontend uses Tailwind CSS 4 with `prettier-plugin-tailwindcss` + `prettier-plugin-css-order` for automatic CSS class and property sorting; these plugins are declared in the frontend's `.prettierrc` and resolved from `frontend/node_modules`. Confidence: 0.65
- Prefers idiomatic Tailwind v4 shorthand syntax over arbitrary-value brackets in class names — migrated `shadow-[var(--shadow-hover)]` → `shadow-(--shadow-hover)`, `size-[18px]` → `size-4.5`, `max-w-[220px]` → `max-w-55` across the UI components in a dedicated refactor pass. Confidence: 0.6
- Commits formatting-only changes as a `style:` type (formatting only, no logic change) and keeps such commits separate from functional changes. Confidence: 0.55
- Prefers the Tailwind class-merge helper (`cn`) to be implemented with `clsx` + `tailwind-merge` — widened to `ClassValue[]` and returning `twMerge(clsx(inputs))` — rather than a plain `filter(Boolean).join(' ')`, so conflicting utility classes resolve deterministically (last wins). Confidence: 0.8
- lint-staged monorepo pattern: per-package `.lintstagedrc.*` files route staged files to run with the package directory as cwd (lint-staged's groupCwd = config dir) for correct node_modules/dependency resolution; root config alone causes plugin resolution failures for package-scoped files. Confidence: 0.6
- When the user says "push," they expect the full fix→commit→push cycle including pushing to the GitHub remote — not just a local commit. Confidence: 0.7
- Wants the `.commandcode` folder (CommandCode's own config/taste directory) treated as part of the project — tracked in git and pushed to the GitHub remote, never left as local-only uncommitted changes. Confidence: 0.75
- Ships the standard open-source community file set at each project's repo root — README.md, LICENSE, CODE_OF_CONDUCT.md, CONTRIBUTING.md, SECURITY.md — and ports/adapts his own versions from prior projects (e.g. page2design) to the new project's stack rather than writing from scratch. Confidence: 0.75
- Wants an individual README inside each monorepo package folder (`frontend/README.md`, `backend/README.md`) in addition to the root README, each tailored "project structure wise" — a folder-structure tree for that side, a table mapping new code to the right folder, plus per-side scripts, stack, and conventions — and cross-linked from the root README so navigation flows both directions. Confidence: 0.65
- When a technology is removed or replaced (e.g. Prisma → Supabase), expects all documentation references — READMEs, folder trees, quick-start steps, scripts tables, stack lists, badges, conventions — to be swept and updated in the same pass, leaving no stale mentions of the old tech. Confidence: 0.65
- Payment provider preference for school-flow-ai: **SSLCommerz** for Bangladesh (BD) domestic payments + **Stripe** for international payments, with Razorpay explicitly rejected/removed. Confidence: 0.9
- Email provider preference for school-flow-ai backend: **Resend** (transactional email API) over **Nodemailer/SMTP**, with `RESEND_API_KEY`/`RESEND_FROM` env vars replacing `SMTP_*`. Confidence: 0.95
- Licenses his projects under MIT. Confidence: 0.7
- README conventions: a shields.io badge row for the tech stack, a prerequisites table, per-package quick-start and scripts tables, a monorepo structure tree, a docs-as-source-of-truth table, and a "Made by" / contact section. Confidence: 0.6
- Wants backend and frontend organized to best-practice, feature-based folder structures — NestJS backend as per-feature modules (each with module/controller/service/dto) plus shared `database/` (Supabase client module, global — replaced the old `prisma/` module) and `common/` (guards/decorators/filters/interceptors); React frontend with `pages/<role>/`, `components/` split into layout/ui/dashboard, and `features/<name>/` folders each holding components + hooks + api.ts, alongside services/, hooks/, context/, types/, lib/, data/, styles/. Confidence: 0.6
- Frontend state management: **Zustand** for global state (auth/session, school context), preferred over Redux. Confidence: 0.9
- Frontend server state: **TanStack Query 5** (`@tanstack/react-query`) for all API/server state — `useQuery`/`useMutation` hooks colocated in feature `api.ts` modules with cache + invalidation; Zustand is for client-only state and must never be used as an API-response cache. Confidence: 0.85
- When a new library/dependency is adopted, expects it recorded across the project's docs in the same pass — Architecture.md tech stack + layer description, Rules.md (Use/Avoid), Memory.md (completed entry + lasting decision), and the CODE_REVIEWER.md checklist — while leaving the product-scope PRD.md untouched (PRD is "what to build for whom", not a place for stack details). Confidence: 0.7
- Frontend animations: **`motion`** (v13) is the animation library — the current package that supersedes `framer-motion`; React APIs are imported from `motion/react` and `framer-motion` (legacy name for the same library) must not be imported directly. Confidence: 0.85
- Frontend date utilities: **date-fns** for date formatting and academic-calendar helpers. Confidence: 0.9
- Frontend PDF generation: **@react-pdf/renderer** for exporting report cards and invoices. Confidence: 0.85
- Prefers UI components hand-rolled from raw HTML + Tailwind + React — explicitly **no third-party component library** (no Radix, Headless UI, shadcn, etc.); primitives are built in-house. Confidence: 0.85
- Wants reusable UI components centralized under `components/ui/` (tab, select, radio, checkbox, card, modal, table, data-table, toast, etc.). Confidence: 0.7
- Expects status feedback in the UI to come as the full semantic tone set — `success` / `warning` / `error` / `info` — exposed as first-class variants on primitives (as with Alert and Toast) and mapped onto the design tokens' soft/strong palette rather than ad-hoc colors. Confidence: 0.55
- Wants design-system components to follow the project's own style guide (Design.md — color tokens, border radii, type scale, focus treatment) rather than generic/default styling. Confidence: 0.6
- Wants database schema documentation as a markdown file in `docs/backend/` (e.g. `Database.md`) written feature-wise to mirror the PRD's feature sections, with **mermaid** diagrams for the visuals (ERD per feature module) rather than a single flat diagram or plain table listing. Confidence: 0.75
- SQL/Supabase schema naming: prefers `snake_case` plural table and column names (Postgres/Supabase idiom, no quoted identifiers in raw SQL), with the service layer mapping to the camelCase JSON the API contract (Design.md) mandates — chosen over matching the PRD's PascalCase/camelCase Prisma-style model names. Confidence: 0.7
- Wants the backend schema doc (`docs/backend/Database.md`) organized **table-by-table**: every table gets its own subsection, each starting with a heading plus a machine-readable metadata comment tag (e.g. `<!-- table: <name> · module: <Nest module> · prd: § · phase: · tenant: · soft-delete: -->`), followed by the full column spec, constraints/indexes, and its own small mermaid ERD of that table's relations — layered on top of the per-feature ERD. Confidence: 0.75
- For schema/feature docs, expects explicit and verified **feature coverage**: every PRD feature maps to at least one documented table and diagram (table inventory + PRD-feature→table coverage matrix), with no orphan tables and no uncovered features. Confidence: 0.6
- Expects the frontend mock data, the mock API routes, and the documented database schema to be kept in **three-way sync**, and asks for this as an explicit audit ("proper sync check") rather than trusting it: each mock entity maps 1:1 onto a documented table, every route (incl. derived read-model/report helpers) is backed by tables that exist, and the schema can actually reproduce the mock's behaviour (enums match, and constraints/keys/sequences like receipt numbering and duplicate guards are really expressible). Wants the gaps listed (missing tables, missing unique/partial indexes, documented semantics not implemented). Confidence: 0.5
- Wants database keys and indexes maintained properly and explicitly in the schema doc, not left implicit — each table documents its primary key and every foreign key with its `on delete` referential action, plus a full per-table index plan (including the tenant/`school_id` index); prefers these consolidated too (an FK map and an index plan) so integrity is auditable and verifiable. Confidence: 0.6
- Prefers markdown-native mermaid diagrams (renders on GitHub, no new tooling or build step) over generated image files (committed SVG/PNG), external services (dbdiagram.io/DBML), or PlantUML — even when asking for a polished/image-style diagram, accepts mermaid as the answer. Confidence: 0.6
- Frontend routing must be organized as explicit route groups — public routes (login/signup/OTP/forgot/reset), private/authenticated routes, and role-restricted groups — each with its own guards (e.g. `ProtectedRoute`, `PublicOnlyRoute`, `RoleGuard`), living in a dedicated `routes/` folder with the route table kept as data rather than a flat list inside `App.tsx`. Confidence: 0.7
- Wants the frontend built and demoable against mock/demo data before the backend exists, so every screen and flow can be exercised end to end without an API — including seed accounts for each role (admin/teacher/student/parent) and demo credentials surfaced in the login UI. Confidence: 0.7
- Mock/demo data lives in a dedicated `data/` folder split into **one file per domain** (school, users, students, classes, fees, notices, …) imported per file — never one monolithic data file — and mirrors the documented backend schema (`docs/backend/Database.md`). Confidence: 0.75
- HTTP client is **axios**, wrapped in a single service layer (e.g. `services/apiClient.ts`) that attaches the JWT, unwraps the `{ success, data, meta }` envelope and normalizes failures into one error type; feature `api.ts` modules are the only callers, so components never touch the client directly. Confidence: 0.7
- Prefers demo/mock data served **through** the real HTTP client via a custom adapter rather than around it, so interceptors, error handling and the query hooks run the identical code path as production, and one env flag (`VITE_ENABLE_MOCKS=false`) switches to the live API with no other change. Confidence: 0.6
- Prefers single-source-of-truth modules over inline literals in the frontend: route strings from `routes/paths.ts`, environment access only through `lib/env.ts` (the one place `import.meta.env` is read), and nav configuration from `lib/navigation.ts`. Confidence: 0.55
- Auth/session state in the frontend: a Zustand store holding the access token in memory only (never `localStorage`), re-acquired on reload from the httpOnly refresh cookie, with guards rendering a spinner (not a redirect) while the handshake is unresolved. Confidence: 0.6
- Keeps the dependency surface minimal and expects new deps to be approved before install — e.g. declines to add a DOM test runner or a global browser tool without asking, and instead reports which behaviours remain unverified. Confidence: 0.6
- Frontend forms: **react-hook-form** is the form state/validation library — wants _every_ form-related component in the project converted to it, not just new ones. Convention applied: `useForm<Values>({ defaultValues, mode: 'onTouched' })`, validation rules declared on `register()` (replacing per-field `useState` + hand-written validators), API failures surfaced via `setError('root.serverError')`, and `formState.isSubmitting` driving the submit button; only genuinely non-field UI state stays in `useState`. Confidence: 0.85
- Adds a new dependency to a package's `package.json` himself (both `frontend/` and `backend/`) and then tells the agent to apply/wire that library across the project or its docs ("I added X ... now update ..."), rather than asking the agent to install it. Confidence: 0.65
- When a library is applied to existing code, expects the project's `components/ui` primitives to be adjusted as needed so the new library integrates cleanly (e.g. input primitives switched to `ComponentPropsWithRef` so react-hook-form's `register()` ref flows through). Confidence: 0.6
- Wants money displayed in **USD ($)** in the frontend UI — explicitly asked for rupee (₹/INR) symbols and lakh/crore magnitudes to be replaced by dollars (`$1,500.00`, `$4.5M`/`$120k`). Confidence: 0.5
- Supplies brand artwork as PNG files dropped into `frontend/public/` (e.g. `school-flow.png`, `mobile-logo.png`) and expects them wired in as the app logo — in the top of the sidebar and on the auth pages — rather than a placeholder/template brand. Confidence: 0.5
- Wants the desktop sidebar to toggle between the full sidebar (labels visible) and an icon-only rail, with a proper animated transition on/off rather than an instant hard switch. Confidence: 0.55
- Prefers cross-cutting UI affordances defined once as global base-layer CSS (e.g. `cursor: pointer` for buttons, `[role="button"]`, `summary`, labels and anchors) rather than hand-applied per component. Confidence: 0.6
- Wants shared layout primitives extracted into a single reusable utility/class used everywhere, not re-declared per page — explicitly asked for one common main-content container class (e.g. a Tailwind `@utility page-container`) and wanted the existing ad-hoc `mx-auto max-w-* px-*` container declarations across shells/sections replaced by it, so width and padding are declared once. Confidence: 0.6
- App-shell layout expectation: the sidebar/chrome must stay pinned and viewport-height while only the main content column scrolls — he explicitly rejects a sidebar that scrolls with the page or stretches with the content (which pushed the account block to the bottom of a long page). Confidence: 0.7
- Prefers **lucide-react** vector icons over raster image assets for compact UI marks — had the sidebar's logo PNG (`BRAND.mark`) replaced with a lucide `School` icon chip, and expects the image asset/brand entry removed (not just hidden) once nothing references it. Confidence: 0.6
- Edits project files directly between turns (e.g. tweaking `AuthShell.tsx` styling himself, or moving the stats row under the header in `TimetablePage.tsx` and adding `justify-end`) and expects the agent to respect those edits — leave them untouched and only flag concerns rather than reverting or "fixing" them. Confidence: 0.6
- Wants interaction states unified on a single design token across the whole app — e.g. every hover background (buttons, nav items, icon buttons, table rows, menu/option highlights) must use the same `bg-primary-soft` tint, with no per-component hover colours; only semantically distinct cases stay different (solid fills darken, destructive items keep the error tint). Confidence: 0.65
- When he states a design/UI convention ("use X everywhere"), expects the agent to sweep the entire codebase and replace the old value globally (all occurrences/files, plus implicit cases that behave the same way), then verify and document it — not just the file/spot he pointed at. Confidence: 0.7
- Expects standard, complete UX affordances on form fields rather than bare primitives — e.g. password inputs masked by default with a show/hide eye toggle (asked for this in the auth forms) — and wants such end-of-field adornments handled once inside the shared `components/ui` input primitive (a `trailing` slot / dedicated `PasswordInput`) instead of ad-hoc per form. Confidence: 0.5
- Wants account creation (student enrolment, teacher add) to send a **verification email**: the new login is provisioned unverified, sign-in is refused (403 `AUTH_NOT_VERIFIED`) until the invite is confirmed, and a verify endpoint closes the loop — and in demo/mock mode the emailed password must be the shared, known demo password rather than a random string nobody can read twice, so the flow is genuinely signable end to end. Confidence: 0.6
  he invite is confirmed, and a verify endpoint closes the loop — and in demo/mock mode the emailed password must be the shared, known demo password rather than a random string nobody can read twice, so the flow is genuinely signable end to end. Confidence: 0.6
- Enrolling a student provisions **two** logins: the student's own email and the guardian's email, both collected on the add-student form — the collected email **is** the account login (`users.email`), never a generated address — each created unverified with its password emailed alongside the invite; no profile table (`students`/`teachers`/`parents`) carries an email column of its own, they all read it from `users`. Frames this as a rule to record in the backend database-schema doc. Confidence: 0.6
- Treats work as spanning multiple sessions and expects the agent to resume it unaided — a bare "finish last session where you left it" should trigger the agent to reconstruct exactly where the previous session stopped (session transcripts, plan files, git status/log/diff, most-recently-modified files) and complete the remaining steps rather than asking him to re-explain; repeated as a one-line resume request that names only the module and the fact the agent stopped it (e.g. "study materials modules where you stop finish it", "finish where you last stop for subject assignment related"). Confidence: 0.7
- Names a popular third-party library as the behavioural spec when asking for a UI change (e.g. "update the toast component as like as react-toastify … animation"), expecting that library's exact motion/interaction details reproduced inside his own hand-rolled primitive rather than the library being installed. Confidence: 0.7
- Code style: **no explanatory comments in code** — the project's `Rules.md` forbids adding them (including doc/JSDoc-style annotations on constants, props and hooks), and any comments the agent introduces are to be stripped before finishing. Confidence: 0.65
- Wants class names in `school-flow-ai` rendered as a bare hyphenated grade-section — `1-A`, `1-B`, `8-A`, `9-B` — produced by one shared helper (`classLabel` / `classLabelFor` in `data/classes.ts`) rather than grade+section concatenated by hand, with each call site adding its own `Class ` prefix only where it reads. Confidence: 0.5
- When asking for a new frontend feature module, also asks for it to be **synced with the backend docs in the same pass** — names the `docs/backend` folder's docs as the targets ("also make sync with data docs/backend folder api and database and architecture"; later "also sync with docs backend folder database, prd, architecture etc"), i.e. the API/PRD, Database and Architecture docs — so any schema column or endpoint the new UI needs (e.g. the assignment form collected Max marks, so `homework.max_marks` was added to Database.md) is written into the backend Database/PRD/Architecture docs and the Memory files alongside the frontend code, not left as an undocumented frontend-only assumption — repeated when the timetable module landed (`periods.label` written into `docs/backend/Database.md` plus PRD §4.8 and the Memory files in the same pass) and again for the notice board (added `notices.priority` + `author_name` and a new `notice_priority` enum to Database.md §2/§11/§19, PRD §3 + §4.11, and both Memory files); for the communication module he asked for it as its own follow-up pass ("under docs/backend folder added route prd.md, database schema, architecture for communication related"), hitting PRD §4.10 (endpoint read models + the unread rule), Database.md §10 (no new columns, notes untangled) and a new Real-time chat section in Architecture.md, plus Memory entries; for the roles & permissions module he asked to "sync with docs/backend folder all files" — the whole folder, so Database.md (new `permissions`/`user_permissions` tables + enum + all cross-sections), PRD.md, Architecture.md, Rules.md, Phases.md and Memory.md all moved in the same pass. Confidence: 0.85
- Wants **soft gradient colour** in the timetable's TIME column — each period row's time cell washed with the palette's soft tints (cycling row by row) instead of a flat label, asked for as "some gradient colors type soft view". Treats it as a sanctioned exception to the project's "no gradients" design rule. Confidence: 0.6
- Wants module pages to close with a **row of stat tiles** summarising that module's numbers — for the timetable, period rows / weekly slots / subjects / teachers tiles across the bottom of the page, requested by pointing at a screenshot. Confidence: 0.5
- Wants repetitive/common form-field input options — the select / radio / checkbox / choice lists that forms keep re-declaring — centralised in one shared options module (`lib/options.ts`) as a single exported list per field, imported by every form instead of retyped inline; shared domain value lists (e.g. subject names) belong in that same module and are imported from it rather than duplicated. Confidence: 0.6
- Sometimes wants a module built as **design only** — the layout/visual shell rendered through the real read path (mock API + hooks, not hardcoded), but with interactive functionality deliberately left unimplemented and the affordances stubbed inert (e.g. the communication composer's send/attach buttons are `aria-disabled` and have no handler) — repeated for the AI Assistant module ("Future i work this module with functionality now generate ux style and style related mock data and docs backend update route, database schema and architecture etc"), where the per-tool action and Copy buttons are likewise left `aria-disabled` — explicitly saying "only design make not apply functionality. In future i will work this module with functionality", i.e. he defers the mutations/writes to a later session he will do himself. Confidence: 0.75
- Prefers tabular listings built on the project's shared `DataTable` primitive (sortable columns, pagination, empty state) rather than a bespoke hand-rolled table — a general preference, not absolute: see the accordion exception below. Confidence: 0.5
- For a list whose rows carry nested detail (e.g. an exam's subject schedule), prefers expandable/accordion rows over flattening that detail into a column — had the Exams tab rolled back from the `DataTable` chips to the previous accordion with an explicit "previous accordion type better ux. rollback it", so when a real detail panel is useful, row expansion beats compressing it into the shared primitive. Confidence: 0.6
- Prefers switching between same-level content within a screen with a row of buttons (one per item, active one highlighted) rather than a `Tabs` strip — rejected the Results screen's subject tabs as poor UX ("need better ux not tab a button type for every subject in header"). Confidence: 0.6
- Wants paired filter controls (e.g. class + subject) laid out row-wise — side by side on one row with fixed widths rather than wrapping onto separate lines. Confidence: 0.55
- Form field layout rhythm: a single standout field (e.g. Date) gets its own full row, with the remaining related fields grouped two-per-row instead of squeezed three-up; the same applies to a form's login/email field, which he pulls out of the two-column row onto its own full-width row (teacher email). Confidence: 0.55
- Wants form inputs to carry a short inline **hint below the field** explaining the field's purpose/consequence rather than relying on the label alone — for login/email fields the hint states what the address is for (e.g. "The guardian's sign-in address — the invite and its password go here."), and the label is made explicit too ("Sign-in email"); when he settles on such a hint/label treatment in one form he expects the parallel field in the other form given the matching treatment. Confidence: 0.5
- Wants a screen's data scoped by the signed-in role ("show data condition wise") — each role sees only its own slice (a teacher their own schedule, a student their class), while the admin alone sees everything and is the only role allowed to edit; all other roles get the same content read-only. Confidence: 0.6
- For schedule/timetable screens, prefers the classic **days × periods grid** (a `TIME | MON…SAT` table, one row per period, today's column highlighted, free cells shown as a dash) over per-day card/day-list layouts — and wants that one board reused across roles rather than a separate bespoke layout per role; rejected the teacher's per-day card list ("no i need ux this type … refactor again") and had it replaced by the same grid as the class week, with cells reading subject over the other party (teacher for a class week, class for a teacher's own week). Confidence: 0.55
- Expects create and edit forms to be equally capable — functionality added to the Add flow must also work when editing. When the agent shipped the subject form's class picker as deliberately **create-only** and flagged the choice, the user asked for it on the edit form too ("In edit subject form should be update class"), so a partial scope the agent ships and offers to extend reads as an incomplete feature, not a safe default. Confidence: 0.5
- Wants the written backend docs reconciled with a feature's **actual data flow** — asks the route/PRD endpoint docs and the Database schema to be audited against what the code really does and the mismatches fixed (e.g. name uniqueness missing from the schema, the in-use guard's table set, a PATCH route contradicting its own behaviour), rather than the docs merely being appended to. Confidence: 0.5
